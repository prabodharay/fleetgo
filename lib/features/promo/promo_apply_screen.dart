
import 'package:flutter/material.dart';

class PromoApplyScreen extends StatelessWidget {
  const PromoApplyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PromoApplyScreen')),
      body: const Center(child: Text('PromoApplyScreen Screen')),
    );
  }
}
