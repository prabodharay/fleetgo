
import 'package:flutter/material.dart';

class OwnerDriverListScreen extends StatelessWidget {
  const OwnerDriverListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('OwnerDriverListScreen')),
      body: const Center(child: Text('OwnerDriverListScreen Screen')),
    );
  }
}
