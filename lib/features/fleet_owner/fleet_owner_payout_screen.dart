
import 'package:flutter/material.dart';

class FleetOwnerPayoutScreen extends StatelessWidget {
  const FleetOwnerPayoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FleetOwnerPayoutScreen')),
      body: const Center(child: Text('FleetOwnerPayoutScreen Screen')),
    );
  }
}
