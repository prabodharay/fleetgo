
import 'package:flutter/material.dart';

class FleetOwnerVehicleScreen extends StatelessWidget {
  const FleetOwnerVehicleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FleetOwnerVehicleScreen')),
      body: const Center(child: Text('FleetOwnerVehicleScreen Screen')),
    );
  }
}
