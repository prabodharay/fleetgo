
import 'package:flutter/material.dart';

class FleetOwnerDashboardScreen extends StatelessWidget {
  const FleetOwnerDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FleetOwnerDashboardScreen')),
      body: const Center(child: Text('FleetOwnerDashboardScreen Screen')),
    );
  }
}
