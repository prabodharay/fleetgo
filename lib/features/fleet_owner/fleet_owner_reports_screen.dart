
import 'package:flutter/material.dart';

class FleetOwnerReportsScreen extends StatelessWidget {
  const FleetOwnerReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FleetOwnerReportsScreen')),
      body: const Center(child: Text('FleetOwnerReportsScreen Screen')),
    );
  }
}
