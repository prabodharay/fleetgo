
import 'package:flutter/material.dart';

class DriverJobRequestScreen extends StatelessWidget {
  const DriverJobRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DriverJobRequestScreen')),
      body: const Center(child: Text('DriverJobRequestScreen Screen')),
    );
  }
}
