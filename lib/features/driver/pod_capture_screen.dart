
import 'package:flutter/material.dart';

class PodCaptureScreen extends StatelessWidget {
  const PodCaptureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PodCaptureScreen')),
      body: const Center(child: Text('PodCaptureScreen Screen')),
    );
  }
}
