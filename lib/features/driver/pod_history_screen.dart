
import 'package:flutter/material.dart';

class PodHistoryScreen extends StatelessWidget {
  const PodHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PodHistoryScreen')),
      body: const Center(child: Text('PodHistoryScreen Screen')),
    );
  }
}
