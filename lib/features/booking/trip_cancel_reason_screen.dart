
import 'package:flutter/material.dart';

class TripCancelReasonScreen extends StatelessWidget {
  const TripCancelReasonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TripCancelReasonScreen')),
      body: const Center(child: Text('TripCancelReasonScreen Screen')),
    );
  }
}
