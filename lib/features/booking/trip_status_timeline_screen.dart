
import 'package:flutter/material.dart';

class TripStatusTimelineScreen extends StatelessWidget {
  const TripStatusTimelineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TripStatusTimelineScreen')),
      body: const Center(child: Text('TripStatusTimelineScreen Screen')),
    );
  }
}
