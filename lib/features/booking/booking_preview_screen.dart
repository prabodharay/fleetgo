
import 'package:flutter/material.dart';

class BookingPreviewScreen extends StatelessWidget {
  const BookingPreviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('BookingPreviewScreen')),
      body: const Center(child: Text('BookingPreviewScreen Screen')),
    );
  }
}
