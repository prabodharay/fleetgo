
import 'package:flutter/material.dart';

class InvoiceBreakdownScreen extends StatelessWidget {
  const InvoiceBreakdownScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('InvoiceBreakdownScreen')),
      body: const Center(child: Text('InvoiceBreakdownScreen Screen')),
    );
  }
}
