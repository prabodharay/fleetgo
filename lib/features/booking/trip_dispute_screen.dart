
import 'package:flutter/material.dart';

class TripDisputeScreen extends StatelessWidget {
  const TripDisputeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TripDisputeScreen')),
      body: const Center(child: Text('TripDisputeScreen Screen')),
    );
  }
}
