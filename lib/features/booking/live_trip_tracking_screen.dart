
import 'package:flutter/material.dart';

class LiveTripTrackingScreen extends StatelessWidget {
  const LiveTripTrackingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LiveTripTrackingScreen')),
      body: const Center(child: Text('LiveTripTrackingScreen Screen')),
    );
  }
}
