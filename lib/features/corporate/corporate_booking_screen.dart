
import 'package:flutter/material.dart';

class CorporateBookingScreen extends StatelessWidget {
  const CorporateBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CorporateBookingScreen')),
      body: const Center(child: Text('CorporateBookingScreen Screen')),
    );
  }
}
