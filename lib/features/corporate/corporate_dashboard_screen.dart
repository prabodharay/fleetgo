
import 'package:flutter/material.dart';

class CorporateDashboardScreen extends StatelessWidget {
  const CorporateDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CorporateDashboardScreen')),
      body: const Center(child: Text('CorporateDashboardScreen Screen')),
    );
  }
}
