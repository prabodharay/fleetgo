
import 'package:flutter/material.dart';

class AdminEarningsDashboard extends StatelessWidget {
  const AdminEarningsDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AdminEarningsDashboard')),
      body: const Center(child: Text('AdminEarningsDashboard Screen')),
    );
  }
}
