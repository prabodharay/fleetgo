
import 'package:flutter/material.dart';

class DistancePricingSettingsPage extends StatelessWidget {
  const DistancePricingSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DistancePricingSettingsPage')),
      body: const Center(child: Text('DistancePricingSettingsPage Screen')),
    );
  }
}
