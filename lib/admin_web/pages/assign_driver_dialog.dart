
import 'package:flutter/material.dart';

class AssignDriverDialog extends StatelessWidget {
  const AssignDriverDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AssignDriverDialog')),
      body: const Center(child: Text('AssignDriverDialog Screen')),
    );
  }
}
