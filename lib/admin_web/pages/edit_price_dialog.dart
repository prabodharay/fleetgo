
import 'package:flutter/material.dart';

class EditPriceDialog extends StatelessWidget {
  const EditPriceDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('EditPriceDialog')),
      body: const Center(child: Text('EditPriceDialog Screen')),
    );
  }
}
