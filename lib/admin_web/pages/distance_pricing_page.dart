
import 'package:flutter/material.dart';

class DistancePricingPage extends StatelessWidget {
  const DistancePricingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DistancePricingPage')),
      body: const Center(child: Text('DistancePricingPage Screen')),
    );
  }
}
