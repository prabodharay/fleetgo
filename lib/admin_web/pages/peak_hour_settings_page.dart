
import 'package:flutter/material.dart';

class PeakHourSettingsPage extends StatelessWidget {
  const PeakHourSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PeakHourSettingsPage')),
      body: const Center(child: Text('PeakHourSettingsPage Screen')),
    );
  }
}
