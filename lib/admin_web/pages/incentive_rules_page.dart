
import 'package:flutter/material.dart';

class IncentiveRulesPage extends StatelessWidget {
  const IncentiveRulesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('IncentiveRulesPage')),
      body: const Center(child: Text('IncentiveRulesPage Screen')),
    );
  }
}
