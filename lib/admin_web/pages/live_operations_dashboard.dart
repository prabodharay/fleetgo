
import 'package:flutter/material.dart';

class LiveOperationsDashboard extends StatelessWidget {
  const LiveOperationsDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LiveOperationsDashboard')),
      body: const Center(child: Text('LiveOperationsDashboard Screen')),
    );
  }
}
