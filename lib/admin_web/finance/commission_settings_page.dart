
import 'package:flutter/material.dart';

class CommissionSettingsPage extends StatelessWidget {
  const CommissionSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CommissionSettingsPage')),
      body: const Center(child: Text('CommissionSettingsPage Screen')),
    );
  }
}
